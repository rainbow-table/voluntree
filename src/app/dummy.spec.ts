import { expect } from 'chai';
import {} from 'jasmine';

// let expect = chai.expect;
describe('Dummy test', () => {
  return it('should crash', () => {
    return expect(false).to.be.false;
  });
});
